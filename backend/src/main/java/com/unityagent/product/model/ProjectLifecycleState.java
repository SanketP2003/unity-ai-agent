package com.unityagent.product.model;

/**
 * Lifecycle states for projects within a workspace.
 */
public enum ProjectLifecycleState {
    CREATED,
    IMPORTED,
    READY,
    ACTIVE,
    ARCHIVED,
    EXPORTED,
    DELETED
}
